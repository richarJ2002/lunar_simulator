"""!
@brief  Report rendering: the shared design system (style), reusable
        Plotly figure constructors (figures), and the HTML page shell,
        navigation and atomic report writing (html).
"""
